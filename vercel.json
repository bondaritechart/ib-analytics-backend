{
  "version": 2,
  "installCommand": "npm install",
  "buildCommand": "npm run build && npm run prisma:deploy",
  "builds": [
    {
      "src": "dist/main.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/main.js"
    }
  ]
}
